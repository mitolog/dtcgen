export enum SketchLayerType {
  Page = 'page',
  Artboard = 'artboard',
  Symbol = 'symbol',
}

// exports names of SketchLayerTypes.
// should be same count with enum count.
export const SketchLayerTypes = [
  SketchLayerType[0],
  SketchLayerType[1],
  SketchLayerType[2],
];
