import UIKit

class {{container.name}}ViewController: UIViewController {
    var viewConfig: ViewConfig?

    override func viewDidLoad() {
        super.viewDidLoad()
        self.view.backgroundColor = UIColor.white

        self.viewConfig = Dtc.config.{{container.name}}
        _ = self.viewConfig!.adopt(name: Dtc.config.baseViewComponentName, on: self)

        /*
         * acknowledgement:
         * // you can refer and customize each views as below.
         * // todo: need to detect which class this view is,
         * // otherwise the app may unexpectedly stopped.
         * let viewId = Dtc.viewId.travelCities.cityList.city.title.value
         * if let title = self.viewConfig?.getView(viewId) as? TextView {
         *     title.text = "foo"
         * }
         */
    }
}
